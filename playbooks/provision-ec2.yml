---
- name: Provision EC2 instance for web server
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - ../vars/aws-vars.yml
  
  roles:
    - { role: provisioning, tags: ["provision"] }
